import { Notification } from './NotInfo.styled';

const NotInfo = () => {
  return (
    <Notification className="notification-item">
      <p className="notification-text">There's nothing here yet!</p>
    </Notification>
  );
};

export default NotInfo;
